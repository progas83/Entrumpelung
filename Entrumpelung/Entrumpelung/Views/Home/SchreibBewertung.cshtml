@model Entrumpelung.Models.CustomerReview

@{
    ViewBag.Title = "SchreibBewertung";
}

<div class="box">
    <div class="turtorialsTittleLine"></div>
    <h2 class="turtorialsTittle">SchreibBewertung</h2>
    <div class="turtorialsTittleLine"></div>
    <div class="turtorialsWrapper">
        <div class="turtorialsItem">
            @using (Ajax.BeginForm("SendReview", null, new AjaxOptions() { HttpMethod = "Post", OnSuccess = "UpdateReviews" }, new { id = "formReview" }))
            {
                <input type="text" name="CustomerName" placeholder="Name" />
                <textarea name="ReviewText" placeholder="Bewertungstext"></textarea>
                <span class="fileTittle">Laden Sie Ihr Foto</span>
                <input type="file" name="Avatar" accept="image/*">
                <span class="fileTittle">Laden Sie ein Foto der geleisteten Arbeit</span>
                <input type="file" name="portfolio" multiple accept="image/*">
                <input type="submit" value="Senden bewertung" name="Subm" />
            }

        </div>
    </div>


            <div class="target">

                @{
                    Html.RenderPartial("ReviewCollectionView", (IEnumerable<Entrumpelung.Models.CustomerReview>)ViewBag.Reviews);
                }

                @*  //("ReviewView", (IEnumerable<Entrumpelung.Models.CustomerReview>)ViewBag.Reviews);

                    @foreach (var r in (IEnumerable<Entrumpelung.Models.CustomerReview>)ViewBag.Reviews)
                     {

                     }*@

            </div>
        </div>
        <script type="text/javascript">
            function UpdateReviews() {
                document.getElementById('formReview').reset();
                // $("#form1").reset;
                $('.target').load('/Updatereviews');
            }
        </script>
